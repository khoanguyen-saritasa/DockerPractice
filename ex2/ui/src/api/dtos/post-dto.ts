// TODO (template preparation): This DTO was made for template. Remove it from your project.
/** Post DTO. */
export interface PostDto {

  /** Post id. */
  readonly id: number;

  /** User id. */
  readonly userId: number;

  /** Title. */
  readonly title: string;

  /** Body. */
  readonly body: string;
}
